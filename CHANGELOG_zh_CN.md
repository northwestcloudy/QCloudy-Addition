# 更新日志

## [0.3.10-beta] - 2026-08-31

适用于 Minecraft 26.1.2 与 26.2 的公开 Beta。本条目包含稳定版 Release 0.3.9 之后完成的全部玩家可见变化。

### 新增

- 加入 Spirit Mask、Bonzo's Mask 与 Phoenix 的精确免死消息识别，以及可选中央提示和三个可独立摆放的冷却 HUD。中央提示总开关与三个 HUD 均默认关闭；Spirit Mask 最长显示 30 秒，Bonzo's Mask 最长显示 360 秒，Phoenix Rekindle 精确显示 60 秒。
- 加入默认关闭的好友组队邀请自动接受，可分别选择普通好友、特殊好友，并提供独立的 16 人白名单。好友数据只从玩家主动打开并浏览的 `/friend list` 页面学习。
- 加入默认关闭的私信组队申请，精确支持 `!p`、`!party` 与 `!invite`；另加入默认关闭的本地 `//invited`、`//invited by` 与 `//i` 快捷私信。
- 加入默认关闭的快速组队指令，只处理 Party Chat 中受支持的 `!` 别名，公屏与公会聊天无法触发。九类子指令默认开启，并可分别限制为仅自己、仅其他队员或所有人。
- 加入默认开启的本地双斜杠组队指令，覆盖 Warp、All Invite、转移队长、踢人、坐标、晋升、Stream、地牢与 Kuudra；未知 `//` 输入不会被 QCA 拦截。
- 加入命令与玩家名补全。合法完整名称与当前观察到队伍中的唯一名称前缀均可使用；歧义或无效前缀不会发送命令。Party Chat 的 `!pt`/`!ptme` 转移给消息发送者，本地 `//pt`/`//ptme` 转移给本机玩家。
- Warp 使用五秒冷却，All Invite 使用两秒冷却；Stream 可接受任意纯十进制人数，`c`、`close` 与 `off` 均可关闭公开组队。
- 加入永久开启、只追踪稳定 Release 的更新提醒。Beta 与 Release 在首次进入世界后每个客户端进程最多检查一次；只有更高序号、Minecraft 版本匹配且严格校验通过的 Release 才会显示一次 Toast 与本地可点击消息。Alpha 不发起请求，QCA 也不会下载或安装 JAR。
- 补齐启动器/HMCL 的官网、源代码与问题反馈元数据，并在 Mod Menu 中显示官网、下载和源代码三个链接。

### 修复与改进

- 修复升级版 Bonzo's Mask 消息 `Your  Bonzo's Mask saved your life!` 未启动冷却的问题。普通版与升级版现在共用同一冷却状态和重复保护。
- 修复 `/friend list` 多页同步，确保已识别好友能够触发自动接受。当前页有效好友立即可用，只有按顺序浏览所有页面后才会清理旧记录；公屏、公会与组队聊天中的近似文本不会污染好友表。
- 修复通用设置目录有时无法滚到底部的问题。
- “兼容模组”现在与其他下级组一样默认收起。
- 为主功能目录、功能设置、兼容性报告和组队白名单加入浏览器式滚动条，支持点击轨道翻页、更宽命中范围，以及拖出轨道后继续移动。
- 没有真实 HUD 的功能不再显示误导性的 HUD 外观设置；没有可编辑设置的卡片不再打开空页面。
- 删除废弃的“安装 Firmament 时交由其处理重复背包功能”设置；物品时间戳与光标位置记忆现在只由各自的 QCA 开关控制。
- 删除废弃空分类、未使用翻译、失效配置字段与退役的背包功能门控代码。
- “聊天”继续作为“通用”下级分类，而不是左侧独立一级分类。

### 兼容与构建

- Beta 0.3.10 仍是可独立运行的纯客户端 Fabric 模组，分别构建 Minecraft 26.1.2 与 26.2；需要 Java 25、Fabric Loader 0.19.3+ 及对应 Fabric API。
- 稳定更新基线仍为 Release 0.3.9 的序号 1。发布本 Beta 不会替换或增加稳定 Release manifest 的序号。
- 统一设置编辑与统一 HUD 编辑仍是默认关闭的实验性概念测试；请备份提供方配置，并在提供方原生编辑器中核对写入。

### Beta 文件

- `QCloudy_Addition-0.3.10+26.1.2-Beta.jar`
- `QCloudy_Addition-0.3.10+26.1.2-Beta-sources.jar`
- `QCloudy_Addition-0.3.10+26.2-Beta.jar`
- `QCloudy_Addition-0.3.10+26.2-Beta-sources.jar`

## [0.3.9] - 2026-08-25

适用于 Minecraft 26.1.2 与 26.2 的稳定 Release。本条目汇总上一个稳定版 2.5.3 之后完成的全部公开变化；下方各 Alpha/Beta 条目继续保留为详细开发历史。

### 自 Release 2.5.3 以来的新增内容

- 加入完全离线的 Attribute Shard 指南，精确覆盖 Bazaar 列出的 320 种 Shard，包含独立图标、符合品质/分类/家族/Skill 的语义颜色、详情、配方、用途、有序输入、数量、候选输出、获取方式、自然与 Fusion 双来源，以及清晰的可点击导航。
- 加入本地 Shard 规划器：目标数量、完整多步 Fusion Tree、候选路线、Materials Only、最快/最便宜、Ironman、Kraken/Kuudra 参数、可编辑每小时获取速度、直接输入输出关系、可拖动 Fusion Lines，以及仅在玩家实际打开 `/hb` 后记录的 Hunting Box 仓库。
- 加入可选价格路线，只读取兼容模组安全暴露的本地缓存价格。QCA 本身不会发送 Bazaar HTTP 请求；没有安全价格提供方时，价格模式保持不可用。
- 加入可配置钓鱼上钩提示音，使用内置 Ciallo 音频，支持水钓与岩浆钓鱼、每根鱼钩只播放一次、独立钓鱼分类、默认关闭与独立音量。
- 加入面向已安装 SkyHanni、Skyblocker、Firmament、BabyZombieAddons 与 Feesh 的功能化统一设置/HUD 管理。设置与 HUD 各有默认关闭的独立总开关、确认窗口、进度页、Refresh、按能力适配版本变化，以及只读“兼容性缺失”报告。
- **实验性警告：**统一设置编辑与统一 HUD 编辑仍是概念测试，默认关闭且尚不稳定。第三方模组更新可能使已识别映射失效；请谨慎使用、备份配置，并在对应模组原生编辑器中核对写入结果。
- 加入 Power Orb 与 Flare 消失提醒。四种本人 Power Orb 使用精确消失聊天；Warning、Alert、SOS Flare 使用确认后的三分钟本地生命周期，支持重新放置重置计时、中央大字、可选音效与默认 64% 音量。
- 加入全部 20 种 Century Cake 的统一追踪、真实世界 48 小时计时、`/cake`、`/centurycakeeffect`、蛋糕头像与悬停信息、单一总开关、合并过期提醒，以及只在玩家点击后发送 `/visit northwestcloudy` 的带下划线续效果操作。
- 加入 Minecraft 26.1.2 与 26.2 独立构建、双语 Wiki/发布校验，以及能够按版本下载的官网页面。

### 修改与优化

- 替换并重新校准 Dwarven Mines 地图。指针改为单一、连续、近似的 X/Z 投影并实时更新，忽略 Y 和记分板小区域跳转，在 The Mist 上方及桥梁位置也会保持显示。
- 重做 Shard 指南与规划器在宽屏、窄屏和窗口缩放时的布局。输入输出按实际内容紧凑排列，点击范围跟随可见内容，搜索框也可在不关闭页面时退出焦点。
- 将 Epic 修正为 Minecraft 深紫色（`§5`），并让 Shard 品质、效果、家族、生物类型、Skill、获取方式与导航颜色对应游戏/Wiki 语义。
- 空 HUD 和空设置分类不再留下空框；钓鱼改用明确的“咬钩提示”子分组，不再出现“钓鱼 → 钓鱼”。
- 可选模组探测改为按需、不可变快照与按能力判断，不再因为版本号未列入白名单而整体失效；未知分支失败关闭，仍可安全识别的分支继续可用。
- 修正未满级 Golden/Jade Dragon 在宠物 HUD 中的等级与经验显示。

### 修复

- 修复重新放置 SOS/Flare 后旧三分钟计时没有重置的问题。现在即使遗漏确认回调，已识别的重新放置也会重启完整生命周期；手持无关物品不会误重置。
- 修复水钓/岩浆钓鱼触发不一致，以及收杆导致同一根鱼钩重复播放 Ciallo 的问题。
- 修复本人 Tree Gift 生物出现时不提醒，同时继续避免其他玩家的公共事件误触发本地提醒。
- 修复 The Park 的 `Jungle Island` 被误认为 Crystal Hollows `Jungle`，导致挖矿 HUD 出现在错误岛屿。
- 修复 Century Cake 首次生效与刷新解析，包括 Starborn Century Cake 的私用区属性图标和正确的 `Hunting Fortune` 名称。
- 修复 Dwarven 指针跳动、消失和不同步、Golden Dragon 错误显示 200 级，以及多处设置/Shard UI 重叠、裁剪、焦点和窄窗口问题。

### 删除或替换

- 从实现、配置、测试和当前文档中删除槽位锁定、Storage Overlay 与菜单中键转换。
- 删除本地设置命令别名 `/aca` 与 `/ca`；保留 `/qca` 与 `/qc`。
- 删除旧 Dwarven 分区限制、Y 层选择和记分板小区域跳转，改用连续 X/Z 投影。
- 删除不完整的 Flare 聊天假设，以及所有距离、增益范围、实体卸载式到期猜测，改用已确认的本地生命周期结束。
- 删除以精确提供方版本白名单作为主要兼容门槛的逻辑，改为逐项能力校验。

### 兼容与安全

- Release 0.3.9 仍是可独立运行的纯客户端 Fabric 模组。可选提供方不是启动依赖，不支持的能力会被省略。
- 需要 Java 25 和 Fabric Loader 0.19.3 或更高版本。Minecraft 26.1.2 使用 Fabric API 0.155.2+26.1.2；Minecraft 26.2 使用 Fabric API 0.154.2+26.2。
- QCA 不会自动移动、点击、钓鱼、Fusion、吃蛋糕、放置 Deployable、重连或代替玩家游玩。本地页面命令不会发送内容；文档中的 `/th`、`/helia` 与 Century Cake 点击续效果都必须由玩家直接操作。

### Release 文件

- `QCloudy_Addition-0.3.9+26.1.2-Release.jar`
- `QCloudy_Addition-0.3.9+26.1.2-Release-sources.jar`
- `QCloudy_Addition-0.3.9+26.2-Release.jar`
- `QCloudy_Addition-0.3.9+26.2-Release-sources.jar`

## [0.2.9-alpha.30] - 2026-08-21

### 修复

- 在 Warning、Alert 或 SOS Flare 仍生效时再次使用已识别 Flare，会立即从本次重新放置开始计算完整三分钟；即使覆盖旧 SOS 时没有再次出现首次放置确认信号，旧到期时间也不会继续触发提醒。
- Flare 放置追踪现在也会读取对方块使用物品的交互；如果第二次物品使用回调漏失，则会用精确的成功放置音效和本地玩家手持的 Flare 恢复确认并重置计时。
- 增加同种 SOS 替换且缺少第二次确认信号、第二次使用回调漏失，以及手持无关物品不得重置计时的回归测试。

### 构建

- 启用新产物命名：`QCloudy_Addition-0.2.9+26.1.2-Alpha-30.jar`。
- Alpha 使用独立迭代号后缀；Beta 与 Release 只保留通道后缀。
- Alpha 30 仅构建 Minecraft 26.1.2。

## [2.9.29] - 2026-08-19

### 修复

- 在 **通用 → 兼容模组** 中明确显示两个互相独立的总开关：**管理其他模组功能设置** 只管理已识别的功能设置，**管理其他模组 HUD** 只管理已识别的 HUD 位置。
- “兼容模组”分组现在默认展开，避免把统一功能设置开关误认为左下角仅编辑 QCA 自身 HUD 的“编辑 HUD”按钮。
- 增加回归测试，强制两个总开关必须同时注册。两者仍默认关闭，各自使用独立的扫描与 Refresh 流程，开启其中一个不会自动开启另一个。

### 构建

- Beta 2.9.29 同时构建 Minecraft 26.1.2 与 26.2。

## [2.9.28] - 2026-08-18

面向 Minecraft 26.1.2 与 26.2 的 Beta 汇总版本，包含 Beta 2.8.17 之后 Alpha 2.8.18–2.8.28 完成的内容。

### 新增

- 加入面向已安装 SkyHanni、Skyblocker、Firmament、BabyZombieAddons 与 Feesh 的可选功能化集成。统一设置与统一 HUD 探测拥有独立总开关、扫描进度页、二次确认、Refresh 和只读兼容性缺失报告。
- 加入 **Power Orb 与 SOS 消失提醒**。本人 Power Orb 读取精确消失聊天；Warning、Alert 与 SOS Flare 使用确认后的本地放置生命周期。提醒不考虑距离、增益范围或实体卸载。
- 加入统一的 **Century Cake 效果过期提醒**，覆盖全部 20 种 Century Cake，并加入 `/cake`、`/centurycakeeffect`、真实世界 48 小时计时、蛋糕头像效果界面、合并过期通知与仅点击后执行的 `/visit northwestcloudy` 续效果链接。

### 改进

- 提供方扫描改为按需、本地、能力探测，并能容忍提供方版本变化；只显示安全识别的分支，缺失或变化的分支会失败关闭。
- 设置分类仅在拥有 QCA 或已发现提供方功能时显示；钓鱼使用明确的“咬钩提示”分组，不再重复顶级分类名称。
- Century Cake 解析支持真实的首次生效与刷新消息，并正确清理 Hypixel 私用区属性图标。

### 修复

- 修复 The Park 的 `Jungle Island` 被误判为 Crystal Hollows `Jungle`，从而在错误岛屿显示挖矿任务与粉尘 HUD 的问题。
- 修复新生效 Century Cake 仍为灰色，并将 Starborn Century Cake 改为 Hypixel 实际使用的 `Hunting Fortune` 名称。
- 删除旧的不完整 Flare 聊天假设；放置失败、冷却误点、换服、距离、增益范围与实体卸载均不会制造错误消失提醒。

### 兼容与安全

- Beta 2.9.28 同时构建 Minecraft 26.1.2 与 26.2。QCA 仍是可独立运行的纯客户端 Fabric 模组；可选提供方不是依赖，不支持的分支会被省略。
- 只有玩家点击 Century Cake 过期消息中带下划线的续效果文字时，才会发送 `/visit northwestcloudy`；不会自动发送任何蛋糕、Deployable、提供方或扫描相关命令。

## [2.8.28] - 2026-08-18

### 修复

- 修复收到精确客户端消息 `Big Yum! You refresh +1<属性图标> Hunting Fortune for 48 hours!` 后，Starborn Century Cake 仍保持灰色的问题。
- 将内置 Starborn 效果和加成从错误的 `Hunter Fortune` 改为 Hypixel 实际使用的 `Hunting Fortune`；匹配前仍会清理私用区属性图标。
- 加入带格式与不带格式的真实刷新消息回归测试。旧的错误拼写 `Hunter Fortune` 不再作为第二个别名被接受。

### 构建

- Alpha 2.8.28 仅构建 Minecraft 26.1.2 版本。

## [2.8.27] - 2026-08-17

### 修复

- 修复首次吃下 Century Cake 后效果仍保持灰色并显示“未生效”的问题，包括 **Starborn Century Cake / +1 Hunter Fortune**。
- 现在会识别 Hypixel 首次生效时的精确消息 `Yum! You gain ... for 48 hours!`，并正确处理 Hunter Fortune 数值中附带的私用区属性图标；原有 `Big Yum! You refresh ... for 48 hours!` 刷新路径继续保留。
- 删除错误的旧假设 `Big Yum! You gain ...`。只有两种真实服务器消息可以创建或刷新 48 小时倒计时。

### 构建

- Alpha 2.8.27 仅构建 Minecraft 26.1.2 版本。

## [2.8.26] - 2026-08-17

### 修复

- 将战斗分类中的设置明确改名为 **Power Orb 与 SOS 消失提醒**，并彻底删除错误的 Flare 聊天判定。
- Power Orb 只读取本人四种精确的 `Your <Power Orb> despawned.` 聊天行。Warning、Alert、SOS Flare 改为独立生命周期：使用精确 Flare 道具后只建立短暂候选，匹配到成功放置音效才开始本地三分钟计时。
- 冷却中误点或放置失败不会启动假计时；成功放置新 Flare 会静默替换旧记录；换世界、换服务器或断线会静默清理；实体卸载、距离和增益范围不会触发或屏蔽提醒。
- 二级设置加入 Power Orb、Flare、中央大字、提示音和 0–100% 音量控制。功能和音效默认开启，默认音量 64%；同一次生命周期结束最多提醒一次。

### 构建

- Alpha 2.8.26 仅构建 Minecraft 26.1.2 版本。

## [2.8.25] - 2026-08-17

### 新增

- 在 **物品与菜单 → Century Cakes** 中加入 **Century Cake 效果过期提醒**。全部 20 种蛋糕效果共用一个默认开启的总开关，不存在分效果开关。
- 客户端收到 Century Cake 刷新消息后会记录真实世界 48 小时倒计时，玩家离线期间也会继续计算。输入 `/cake` 或 `/centurycakeeffect` 可打开仿原版 `/effects` 的只读菜单，查看真实蛋糕头像、加成、品质和剩余时间。
- 单个效果到期时显示中央大字，并在本地聊天栏显示 `[QC] Century Cake <Effect> Expired! Click Here For Cake Eating`；同批多个效果到期会合并为一条，例如 `[QC] 8 Century Cake Effect Expired! Click Here For Cake Eating`。
- 带下划线的 `Click Here For Cake Eating` 只有在玩家点击后才会执行精确指令 `/visit northwestcloudy`。该功能拥有独立本地音效，默认开启且音量为 64%。

### 安全边界与构建

- 蛋糕追踪只读取客户端收到的聊天并保存本地绝对到期时间；不会查询 Hypixel、自动吃蛋糕或自动发送命令。
- Alpha 2.8.25 仅构建 Minecraft 26.1.2 版本。

## [2.8.24] - 2026-08-16

### 新增

- 在 **战斗 → Deployables** 中加入第一版 **Deployable 到期提醒**。其中不完整的 Flare 聊天判定已在 2.8.26 中被确认生命周期实现彻底替换。
- 为此功能加入独立音效开关与 0–100% 音量滑条；音效默认开启，音量默认 64%。

### 安全边界与构建

- 本功能不会发送聊天、命令、数据包、交互或网络请求。
- Alpha 2.8.24 仅构建 Minecraft 26.1.2 版本。

## [2.8.23] - 2026-08-16

### 修复

- 修复 The Park 的完整地点名 `Jungle Island` 被误判成 Crystal Hollows 的 `Jungle` 子区域，导致挖矿任务与粉尘 HUD 在非挖矿岛屿显示的问题。
- 完全隐藏没有任何可用功能的顶级设置分类。Dungeons 等分类只有在 QCA 自身或已安装的兼容提供方确实提供至少一个可读取功能时才会出现。
- 将重复的“钓鱼 → 钓鱼”下级分组改为明确的“咬钩提示”。

### 构建

- 按照 Alpha 通道规则，Alpha 2.8.23 仅发布 Minecraft 26.1.2 版本。

## [2.8.22] - 2026-08-15

### 新增

- 每一次可选提供方能力扫描前都会显示第二次确认窗口。窗口会明确区分“统一设置编辑”或“统一 HUD 编辑”的扫描范围，并说明扫描只在本地只读执行、可能短暂增加 CPU 占用。
- 首次开启没有有效会话快照的编辑器，以及玩家点击对应编辑器的 Refresh 时，都必须先完成确认。

### 更改

- 取消首次确认会保持总开关关闭，且不会创建扫描任务；取消 Refresh 会继续保留上一份已经校验的快照。
- Minecraft 重启后，即使总开关仍保持开启，也不会静默扫描提供方。只有玩家明确确认后才会开始扫描；已有扫描运行时 Refresh 不可点击。
- Alpha 2.8.22 分别为 Minecraft 26.1.2 与 26.2 构建。

### 安全

- 本次确认流程只改变本地界面和扫描调度。提供方探测仍为确定性只读过程，不请求服务器或 HTTP，不发包、命令、聊天或遥测，不模拟输入，也不直接修改提供方配置文件。

## [2.8.21] - 2026-08-15

### 新增

- 将 **Feesh** 作为第五个可选提供方接入相互独立的“统一设置编辑”“统一 HUD 编辑”、可视化扫描进度与“兼容性缺失报告”。
- 加入基于能力的 Feesh 设置探测。QCA 只读取拥有成对公开 getter/setter 的 Kotlin 委托属性，只对仍未分类的功能执行本地分类，并在成功写入后调用 Feesh 自己的 `Settings.save()` 保存路径。
- 从 Feesh 的实时 Overlay 注册表中发现 HUD。只有已启用、当前确实会绘制且存在非空内容的面板才进入“编辑 HUD”；位置、缩放和对齐会按照 Feesh 的原生锚点模型换算，并通过 Feesh 自己的坐标存储持久化。

### 兼容性与安全边界

- Feesh 适配没有精确版本白名单，也不是编译或运行依赖。普通版本更新后仍兼容的能力可以继续使用；变化或不透明的设置/HUD 会从编辑器中省略，并按提供方进入兼容性缺失报告，不会隐藏正常的同级功能。
- QCA 不调用 Feesh 的 API、聊天、命令、分享或玩法功能。修改 Feesh 自己的选项只会使用其正常 setter 与保存契约；玩家明确选择的 Feesh 设置之后产生的行为仍由 Feesh 自身负责。
- 两个统一编辑总开关仍然相互独立且默认关闭；只有启用其中一个或玩家点击 Refresh 时才会扫描。
- Alpha 2.8.21 分别为 Minecraft 26.1.2 与 26.2 构建。

## [2.8.20] - 2026-08-15

### 新增

- 为相互独立的“统一设置编辑”和“统一 HUD 编辑”总开关加入按需能力扫描。启用任一开关后会直接打开扫描页面，以进度条显示当前提供方、阶段、正在处理的内容和最近活动。
- 在两个统一编辑页面中分别加入 **Refresh**。重新扫描期间继续使用上一份有效快照，只有新的只读扫描完成并通过校验后才会原子替换，避免不完整状态覆盖正常结果。
- 对经过原生路径和已验证规则后仍未分类的提供方功能，加入一个小型、确定性的本地元数据分类器。它只使用固定本地权重和置信度门槛；置信度不足时保留为未分类，不强行猜测。

### 改进

- 设置与 HUD 探测共用一份不可变会话快照，但开关、数量和界面结果保持独立；设置页只统计可管理设置，HUD 页只统计可管理 HUD。
- 扫描只会在开启总开关或玩家主动点击 Refresh 时发生。打开设置菜单不再无条件重新扫描；两个总开关都关闭时会取消待处理扫描并释放会话快照。
- 扫描结果只显示已经安装且成功产出可读取能力的提供方，不显示未安装模组。探测仍按单项能力安全降级，某个分支变化不会让整个提供方的兼容内容消失。
- 提供方工作拆分到不同客户端 tick，并实时说明当前正在扫描哪个已安装模组及哪个阶段。

### 安全边界

- 探测与分类完全在本地只读完成，结果可复现。本次更新没有加入云端 AI、网络请求、服务器查询、数据包、命令、聊天、遥测、自动输入或直接编辑配置文件。

## [2.8.19] - 2026-08-15

### 改进

- 缓存按提供方分组的兼容性缺失报告布局和已经换行的文本。每次打开报告仍会执行新的只读能力检查，但正常渲染时不再每帧重复构建相同分组和文本布局。
- 将 Lasso `REEL` 检测缩减为每次有效检查只遍历一遍已加载实体，并且只有确认存在本地玩家的套索目标后才解析 ArmorStand 名称。
- 删除 Shard Fusion 索引中从未被读取的重复“全部配方”引用列表；精确输入、产物和用途索引继续共享同一组不可变配方对象与结果。
- 在 Gradle 配置阶段固定 Fabric Loader 元数据，资源处理不再调用已弃用的执行期 `Task.project` API；项目脚本此前产生的 Gradle 10 兼容性警告已经消除。
- 明确从可运行 JAR 与 Sources JAR 中排除 Finder `.DS_Store` 元数据。

### 兼容性与安全边界

- 功能默认值、配方结果、提供方保存行为、HUD 语义、命令内容与网络行为均未改变；QCA 仍可独立运行且保持纯客户端。
- Alpha 2.8.19 分别为 Minecraft 26.1.2 与 26.2 构建并验证。

## [2.8.18] - 2026-08-15

### 新增

- 在 **通用 → 兼容模组** 中新增只读的 **兼容性缺失报告** 卡片。它与普通功能卡片使用不同的视觉样式，没有开关、启用状态或蓝色启用条，鼠标左键和右键都可以打开。
- 新增按 SkyHanni、Skyblocker、Firmament、BabyZombieAddons 分组的报告。只有“已明确识别、但无法统一管理”的功能才会出现，并标注 `[设置]`、`[HUD 编辑]` 或两者；能够正常管理的功能不会进入报告。

### 改进

- 打开报告时会执行一次新的只读能力检查，不受两个默认关闭的统一编辑总开关影响。已安装模组的配置根无法读取或识别结果为空时，不会再错误显示为“全部支持”。
- QCA 能识别名称但无法安全写入的复杂颜色、组合按键或位置结构会继续从普通编辑器中隐藏，同时显示在兼容性报告中。对完全未知的未来结构不会随意编造功能名称。

### 通道与安全边界

- 按要求将后续开发版本切回 **Alpha**；本版本为适用于 Minecraft 26.1.2 与 26.2 的 `Alpha 2.8.18`。
- 报告只读取已安装客户端模组的运行时结构，不修改其他模组数值或配置文件，也不发送服务器数据包、命令、聊天、HTTP 请求或遥测。

## [2.8.17] - 2026-08-15

### 改进

- 在“通用”中加入相互独立的“统一设置编辑”和“统一 HUD 编辑”总开关。两者均默认关闭，可以分别启用，不影响 QCA 自己的设置或 HUD。
- 删除 SkyHanni、Skyblocker、Firmament 与 BabyZombieAddons 的精确版本白名单，改为能力探测。其他模组更新后，只要实时配置对象和保存接口仍兼容，QCA 认识的设置和 HUD 坐标就可以继续显示和编辑。
- 加入逐字段的防御式配置扫描：只显示可以确认安全读写的开关、枚举、有明确范围的数值以及已识别 HUD 位置；QCA 不认识的新结构会被跳过，不会让整个提供方消失，也不会阻止 QCA 设置界面打开。
- 加入带功能前缀的开关与 HUD 坐标识别。例如 `enabledCommissions` 对应的 `commissionsX`、`commissionsY`、`commissionsScale` 不再需要专门写死版本字段表。
- 启用对应总开关后，每次打开统一设置界面都会重新探测提供方能力，避免其他模组尚未完成初始化时产生的不完整扫描结果一直被缓存。

### 删除

- 删除本地设置命令 `/aca` 与 `/ca`。当客户端命令名称未被占用时，仍可使用 `/qca` 与 `/qc`。

### 兼容边界

- 这属于尽力而为的结构兼容，并不代表 QCA 会自动理解未来版本的所有新功能。只有能读取、能以受支持类型安全写回、并能调用提供方自身保存路径的内容才会出现；未识别的新功能仍保留在原模组中，在 QCA 更新适配前不会显示。
- QCA 不会直接修改其他模组的配置文件；本次变化没有新增服务器数据包、命令、聊天、HTTP 请求、玩法自动化或提供方运行依赖。

## [2.7.17] - 2026-08-14

适用于 Minecraft 26.1.2 与 26.2 的 Beta 整合发布，汇总 Beta 2.6.6 之后已完成的全部变化。

### 新增

- 新增默认关闭的“钓鱼上钩提示音”，拥有独立 0–100% 音量控制，默认 64%。支持直接归属于本地玩家的水钓鱼钩与 Hypixel 有限关联的 owner 缺失岩浆鱼钩，每次确认上钩最多播放一次。
- 在原有离线 320-Shard Fusion Guide 之外新增本地 Shard Planner：多步 Fusion Tree、直接候选路线、Materials Only 汇总、可编辑获取速度、最快/最便宜路线、Ironman 模式、Kraken/Kuudra 参数、可拖动 Fusion Lines，以及仅从玩家亲自打开页面记录的按 Profile Hunting Box 仓库。
- 新增可选 Skyblocker 公开缓存价格 API 路线。QCA 不发起价格 HTTP 请求；未安装已审核提供方时，价格路线会明确显示不可用。
- 新增首个以功能为中心的统一设置/HUD 层，支持已审核的 SkyHanni 7.41.0、Skyblocker 6.8.2、Firmament 44.3.0 与 BabyZombieAddons 3.4.1。完全等价功能可选择唯一提供方，已校验的原生数值通过对应模组自己的路径保存，未支持版本会安全关闭。
- 新增双版本构建矩阵和一键脚本，分别生成 Minecraft 26.1.2 与 26.2 的可运行 JAR 和 Sources JAR。

### 改进

- 将“钓鱼”提升为独立一级分类，并让较矮界面中的分类行距自适应。
- 重做 Shard Planner、Shard 详情、Settings、Fusion Lines、RGB 选择器、功能二级页和 HUD 编辑器的窄屏布局；较长中英文本可完整换行/滚动，裁剪与点击范围一致。
- 替换矮人矿洞背景，并将玩家箭头改为整张单层总览共用的连续、大致 X/Z 投影；Y 和计分板子地点不影响箭头。

### 修复

- 修复 Hypixel 有限关联岩浆鱼钩不播放上钩提示音，以及玩家收杆时提示音重复播放的问题。
- 修复属于本人的 Tree Gift 生物提示（如 `A wild Groundhog appeared!`）被丢弃的问题，同时继续拒绝附近其他玩家的公共行。
- 修复未满级 Ancient Golden Dragon 被显示为 `[Lvl 200]`；只有收到的精确总经验达到真实满级门槛后才启用溢出等级。
- 修复受支持的紧凑布局下 Shard 详情/速度控件、Planner 控件、Fusion Line 节点、Settings 字段、RGB 控件与 HUD 编辑行重叠的问题；被裁剪的行不再保留不可见点击区。
- 修复 The Mist 上方桥梁等垂直重叠路径中矮人矿洞箭头跳区或消失，新增 `C&C Minecarts Co.` 识别，并保证超出范围的箭头仍安全位于总览内。

### 替换

- 删除旧的矮人矿洞计分板子地点吸附与分区内裁剪逻辑。Beta 2.6.6 中的用户功能没有被删除；旧定位方法已被连续实时 X/Z 投影取代。

### 兼容性与安全边界

- QCA 仍是可独立运行的纯客户端 Fabric 模组；没有新增自动点击、Fusion、钓鱼动作、移动、战斗、抓捕、数据包、聊天、命令、HTTP、遥测或隐藏服务器数据请求。
- QCA 自身功能同时构建为 Minecraft 26.1.2 与 26.2。第三方设置/HUD 精确版本适配目前只审核了 26.1.2；26.2 中会主动安全关闭，直到完成对应提供方构建的审核。
- `/th` 与 `/helia` 仍是唯一明确记录的用户手动服务器命令快捷方式；没有玩家直接输入就不会执行。

## [2.6.17] - 2026-08-13

适用于 Minecraft 26.1.2 与 26.2 的 Alpha 矮人矿洞总览图同步修复。

### 修复

- 删除矮人矿洞箭头对计分板子地点的选区和分区内强制裁剪。现在整张单层背景使用一套连续的大致 X/Z 映射，因此位于 The Mist 上方的桥梁和其他垂直重叠路径不会再让箭头跳到另一个命名区域。
- 将官方子地点 `C&C Minecarts Co.` 加入矮人矿洞岛屿识别，进入该区域时地图不会再被错误卸载。
- 矮人矿洞地图坐标行现在只显示 X/Z。投影 API 完全没有 Y 参数，Y 无法直接或间接改变箭头。
- 超出校准范围的坐标仍会把完整箭头安全限制在背景内，并新增连续单轴移动、The Mist 桥梁重叠、代表区域和边界裁剪回归测试。

## [2.6.16] - 2026-08-13

适用于 Minecraft 26.1.2 与 26.2 的 Alpha 双版本平台更新。

### 新增

- 新增双版本矩阵：同一套 QCA 功能会分别使用对应的 Fabric API 与可选 Mod Menu 版本构建为 Minecraft 26.1.2 和 26.2 产物。
- 新增 `tools/build_all_versions.sh`，一次运行即可测试并准备两个可运行 JAR 与两个 Sources JAR。

### 兼容性

- 适配 Minecraft 26.2 的界面读取/切换、Overlay 检查、HUD 隐藏状态、中央标题、聊天滚动、Tab 读取与方块中心距离计算，同时通过很薄的目标版本适配层保留 26.1.2 原行为。
- 模组仍为纯客户端；本次适配没有新增数据包、命令、点击、自动操作、HTTP 或服务器数据读取。
- 现有 SkyHanni、Skyblocker、Firmament 与 BabyZombieAddons 精确版本适配仍只对 26.1.2 完成审核；26.2 中会安全关闭，直到对应提供方版本通过审核。

## [2.6.15] - 2026-08-12

适用于 Minecraft 26.1.2 的 Alpha 响应式 UI 修复。

### 修复

- 重做 Shards 详情页布局：标题、属性、获取方式、获取速度输入框、保存与重置按钮现在统一使用右侧详情栏，不再相互覆盖。
- Shard 搜索结果与右侧详情文字分别支持滚动；较长的效果和获取说明会在裁剪区域内完整换行，底部速度设置保持固定。
- Planner 控件会在碰撞前自动重排；窄 Settings 改为单列；Fusion Lines 使用可滚动画布，不再把溢出节点挤在同一行。Settings 高度不足以安全容纳控件时会提示增大界面高度，不再把输入框画到面板外。
- 修正主设置界面、功能二级菜单、RGB 颜色选择器与 HUD 编辑器工具栏的响应式尺寸和文字适配；已裁掉的设置行不再残留不可见点击区。

### 验证

- 新增确定性布局测试，覆盖宽/窄 Shards 页面、Planner 控件、Settings 列布局与 Fusion Lines 画布扩展。

## [2.6.14] - 2026-08-12

适用于 Minecraft 26.1.2 的 Alpha 宠物等级显示修复。

### 修复

- 修复未满级、佩戴 Ancient Golden Dragon Skin 的 Golden Dragon 被错误显示为 `[Lvl 200]`。现在只有客户端收到的精确总经验真正达到 200 级满级门槛后，才会启用皮肤支持的溢出等级显示。
- 无法取得精确经验时，普通未满级宠物会保留 Hypixel 发来的真实等级，不再因溢出等级回退逻辑被强制提升到满级。

### 保持不变

- 真正满级的 Ancient Golden Dragon 仍显示 200 级；启用对应选项后，已经验证超过 200 级门槛的经验仍会显示受支持的皮肤溢出等级。

## [2.6.13] - 2026-08-12

适用于 Minecraft 26.1.2 的首个统一 SkyBlock 模组控制 Alpha。

### 新增

- 新增以功能为中心的统一设置注册表，覆盖 QCloudy_Addition 与已审查的 SkyHanni 7.41.0、Skyblocker 6.8.2、Firmament 44.3.0、BabyZombieAddons 3.4.1。所有集成都可选，QCA 仍可独立启动与运行。
- 一级分类固定为：通用、地图、物品与菜单、战斗、地牢、Slayer、挖矿、种地、砍树、钓鱼、狩猎、Rift、活动。Safari 归入狩猎，Garden 归入种地，Crimson Isle/Kuudra 归入战斗；一个功能只出现一次。
- 共享功能的二级菜单第一项加入提供方选择。选中提供方并开启统一卡片时，只开启所选实现，并关闭其他已检测模组中完全等价的实现；用途相关但不相同的功能不会被错误联动。
- 对兼容提供方加入实时原生布尔、枚举、有边界数值、位置与缩放控制。数值从对应模组运行时配置读取并写回，通过其原生保存路径持久化；模组未加载时，QCA 不会直接改写它的 JSON。
- QCA 原有“编辑 HUD”界面现在可显示所选提供方中已启用的第三方 HUD。拖动/缩放时只更新预览，松开鼠标才写回对应模组自己的位置或缩放，避免逐帧保存。

### 安全与兼容

- 集成使用反射并锁定到已审查的具体版本。缺失、版本不兼容或结构变化的提供方会被隐藏，不猜字段、不强写。
- 首个 Alpha 只展示能够验证并安全持久化的设置。自定义颜色对象、复合快捷键对象等尚未审查的复杂原生编辑器仍需在对应模组自己的界面中修改。
- 统一控制不会下载数据、发送数据包或命令、点击菜单，也不会让 QCA 对其他 SkyBlock 模组产生硬依赖。

## [2.6.12] - 2026-08-11

适用于 Minecraft 26.1.2 的 Beta 钓鱼提示音与设置分类修复。

### 修复

- 修复玩家收杆时内置 Ciallo 上钩提示音会再次播放的问题。现在会把玩家真实使用钓竿区分为“新抛竿”和“收杆”；只有确认的新抛竿才会重新打开每根鱼钩一次的播放门。
- 保留精确 `!!!` 上钩标记、直接水钓鱼钩优先、有限 owner 缺失岩浆鱼钩关联、每根鱼钩去重、默认关闭及独立默认 64% 音量设置。

### 调整

- 将“钓鱼”从“通用”的下级功能组提升为独立一级设置分类，顺序位于“砍树”和“狩猎”之间。
- 八个一级分类在较矮 GUI 中会自动压缩侧栏行距，避免新增“钓鱼”与底部按钮重叠。

### 安全边界

- 本次变化只判断玩家真实触发的钓竿使用属于抛竿还是收杆，并播放客户端本地声音；不会自动抛竿、收杆、取消输入、点击、移动、发送聊天、命令或额外数据包。

## [2.6.11] - 2026-08-11

适用于 Minecraft 26.1.2 的 Beta Shard 路线规划更新。

### 新增

- 完整保留原有的离线 320-Shard Fusion Guide，并新增独立的游戏内 **Shard Planner**：可输入目标与数量，生成完整多步 Fusion Tree，查看直接配方候选，并可切换 Materials Only 只汇总材料。
- 新增根据可编辑“每小时获取数量”计算的**最快路线**，以及根据可选客户端 Bazaar 缓存计算的**最便宜路线**。Normal 模式可以比较狩猎和购买时间；Ironman 模式永远不使用 Bazaar。
- 新增只读 Hunting Box 仓库。只有玩家亲自打开客户端已经收到的 `/hb` Hunting Box 页面时，QCA 才记录 Shard ID 与 `Owned: N Shards`，按 Profile 保存在本地，并用保存数量抵扣规划材料。
- Planner 独立提供输入/输出直接配方筛选、完整 Shard 效果/家族/Skill/获取方式与自定义速率、可拖动 Fusion Lines、仓库查看和本地设置页面。
- Kraken 规划加入 Kuudra Tier、通关时间、coins/hour 机会成本、Hunter Fortune、Crocodile 等级和单次 Fusion 操作时间参数。

### 兼容性

- Bazaar 价格属于可选、无硬依赖兼容。安装兼容 Skyblocker 时，QCA 可通过其公开 `ItemUtils.getItemPrice` API 读取 Skyblocker 已经缓存的价格。
- SkyHanni 与 Firmament 当前没有稳定的跨模组 Bazaar 价格公开 API，因此不会被当成价格提供者。如果没有兼容提供者，基于价格的“最便宜”规划会明确显示不可用；离线 Guide、Ironman/速率路线、仓库、配方、详情和 Fusion Lines 仍可使用。

### 安全边界与保存

- Planner 的速率、图节点位置、模式、目标、数量与 Kuudra 参数保存在 QCA 本地配置；仓库使用单独的按 Profile 本地 JSON。
- QCA 不发起价格 HTTP/Wiki 请求，不自动输入 `/hb`，不点击容器、不执行 Fusion、不选择产物，也不发送数据包、聊天、移动或自动操作。它只读取打包数据、其他客户端模组已缓存的可选数据，以及玩家实际打开过的菜单。

## [2.6.10] - 2026-08-11

适用于 Minecraft 26.1.2 的 Beta Tree Gift 归属与生物提示修复。

### 修复

- 修复 `-A wild Groundhog appeared!` 等已启用 Tree Gift 生物行虽然被正则识别，却被归属状态机静默丢弃的问题。
- 本人归属现在以只发给玩家本人的 `+N rewards gained!` 汇总作为凭证，不再额外强制依赖旧格式 `You helped cut...` 贡献句；附近玩家只有公共生物行时仍然无法触发提示。
- Tree Gift 结束边框后继续保留 5 秒、仅限本次已证明本人归属的生物提示窗口，从而覆盖 Hypixel 在区块结束后发送的生物生成行，同时不会放宽为无限公共聊天检测。
- 支持完整 Tree Gift 以单个多行聊天 Component 到达，也支持聊天压缩后在同一个无边框 Component 中同时保留本人汇总与生物行。
- 无论本人汇总出现在生物/奖励行之前还是之后，待确认内容都会正确结算；同一礼物中的相同稀有物仍然只提示一次。

### 安全边界

- 修复只读取客户端已经收到的聊天文字与 `SHOW_TEXT` hover，不发送数据包、聊天、命令、点击、移动或服务器请求。

## [2.6.9] - 2026-08-11

适用于 Minecraft 26.1.2 的 Beta 岩浆钓鱼兼容性修复。

### 修复

- 修复部分 Hypixel 岩浆钓鱼抛竿后“钓鱼上钩提示音”不播放的问题。水钓仍优先使用原版直接归属于本地玩家的鱼钩；玩家真实右键使用钓竿后，QCA 现在可以在短时间内关联岩浆钓鱼表现中新增但 owner 暂时为空的 Fishing Hook。
- 回退关联会排除抛竿前已经存在的鱼钩和明确属于其他玩家的鱼钩，并持续跟踪同一根已关联鱼钩，直到它消失或玩家收杆。

### 性能与安全边界

- 较大范围的鱼钩查找仅在 40 tick 的有限关联窗口内，或已关联回退鱼钩仍加载时运行；空闲状态不会每 tick 扫描鱼钩。
- 检测仍是被动、本地功能：不会抛竿、收杆、点击、移动、取消钓竿使用，也不发送数据包、聊天或命令。

## [2.6.8] - 2026-08-11

适用于 Minecraft 26.1.2 的 Beta 客户端音频更新。

### 新增

- 在“通用 > 钓鱼”中加入默认关闭的“钓鱼上钩提示音”。功能只识别本地玩家自己的 Fishing Hook 附近、精确显示 `!!!` 的可见 ArmorStand，并且每次抛竿只播放一次内置 Ciallo 提示音。
- 加入该功能独立的 0–100% 连续音量滑块，默认 64%。

### 安全边界

- 功能默认关闭，只扫描本地玩家已加载鱼钩周围四格范围；不会自动收杆、点击、移动、发送数据包、聊天或命令。
- 提供的 MP3 已转换为模组内置资源包中的 OGG；播放完全在客户端本地完成，不需要额外材质包或运行时网络请求。

## [2.6.7] - 2026-08-10

适用于 Minecraft 26.1.2 的 Beta 地图更新。

### 调整

- 将内置 Dwarven Mines 地图替换为本次提供的单层 12 区域地图。
- 按新图的实际区域位置重新校准全部 Dwarven 地点，使实时玩家箭头与对应区域及局部 X/Z 位置同步。
- Dwarven 地图完全移除 Y 轴判定。现在只使用已收到的子地点、本地 X/Z 和朝向；只显示通用 `Dwarven Mines` 时，使用最近的 X/Z 区域中心回退。
- Beta 补丁版本提升至 `2.6.7`；可运行和源码产物统一使用 `QCloudy_Addition-Beta-2.6.7+26.1.2`。

### 安全边界

- 地图仍为纯客户端、纯渲染；不读取隐藏地形，不发送数据包、聊天、命令、点击、移动或其他服务器交互。

## [2.6.6] - 2026-08-10

适用于 Minecraft 26.1.2 的 Beta 晋级版本。

### 调整

- 将已经审核的 `2.5.6` 功能集从 Alpha 晋级为 Beta，没有新增玩法自动化或服务器交互。
- 按照“Beta 更新第二位版本数字”的项目规则，将版本号更迭为 `2.6.6`。
- 可运行产物统一命名为 `QCloudy_Addition-Beta-2.6.6+26.1.2.jar`，源码产物命名为 `QCloudy_Addition-Beta-2.6.6+26.1.2-sources.jar`。
- 同步更新 GitHub、Modrinth、实现说明、验证报告和发布清单。
- 按实际七个一级设置分类、当前 Beta 范围、依赖、HUD 自定义与明确的客户端/服务器命令边界，完整重写 Modrinth 项目描述。

### 包含 2.5.x Alpha 开发线内容

- 面向官方 320 种 Shard 的独立离线 Attribute Shard Fusion Guide，包含合成来源、可合成内容、详细信息、有序输入、数量、可选产物、获取方式、语义颜色和专属图标。
- 汇总 Alpha 2.5.4 至 2.5.6 的搜索焦点、紧凑配方布局、Epic 品质颜色、可点击链接、Wiki 格式清理、反向配方及自然来源与 Fusion 来源并存等修复。
- 彻底删除槽位锁定、Storage 覆盖与菜单中键转换。

### 安全边界

- Beta 仍是纯客户端、被动辅助。Shard Guide 运行时不请求 Wiki/API，不发送数据包，不点击物品栏，不执行 Fusion，不发送聊天、服务器命令或自动操作。
- `/th` 与 `/helia` 仍是合规文档中明确披露的玩家主动快捷命令；没有玩家直接输入时不会发送命令。

## [2.5.6] - 2026-08-10

适用于 Minecraft 26.1.2 的 Alpha 更新。

### 新增

- 为 320 个 Shard 全部加入独立的“详细信息”页面，显示 Wiki 当前列出的完整效果、品质/分类/Skill/家族/生物类型，以及每一种已记录获取方式。捕捉来源保留生物、工具与区域；击杀、掉落、Fusion、Tree Gift、商店和宝箱来源保留 Wiki 已提供的细节，不虚构未记录的概率。
- 对所有可由 Fusion 产出的 Shard 显示已验证有序配方数量；Queen Bee 这类同时拥有自然来源和 Fusion 配方的 Shard 也包含在内。只能通过 Fusion 获得的 Shard 会单独标明。

### 修复

- Epic Shard 名称从粉色/亮紫色（`§d`）更正为 Minecraft 的 Epic 深紫色（`§5`）。属性、分类、生物类型、获取方式与品质文字按 SkyBlock/Minecraft 对应语义颜色显示。
- 可点击的 Shard 文字仅在鼠标真正悬停于可见文字时变深并添加下划线，让跳转入口清晰，同时不缩小原有点击范围。
- 保留不同颜色效果片段之间的空格，并清除离线目录中残留的 Wiki 格式标记。

### 调整

- Alpha 版本提升至 `2.5.6`，产物名为 `QCloudy_Addition-alpha-2.5.6-26.1.2.jar`。
- 320-Shard 离线详情目录已按当前 Wiki 品质表修订版及官方 Bazaar 允许列表更新；运行时仍然完全本地、只读。

## [2.5.5] - 2026-08-10

适用于 Minecraft 26.1.2 的 Alpha 更新。

### 修复

- 用 320 个与具体 Shard 对应的内置图标替换通用紫水晶回退。客户端已经收到的原生 Shard `ItemStack` 仍然优先，并保留在本次会话缓存中；存在服务器/材质包原生显示时继续以其为准。
- 点击搜索框外、按 `Esc` 或按 `Tab` 现在都会退出搜索焦点；再次点击搜索框即可重新输入。无需关闭 Guide 即可恢复配方导航与普通界面快捷键。
- 每张配方卡的两个输入与输出集合改为按内容宽度紧凑居中；点击范围跟随实际可见物品，不再分散到卡片两端。

### 调整

- Alpha 版本提升至 `2.5.5`，发布文件名为 `QCloudy_Addition-alpha-2.5.5-26.1.2.jar`。
- 320 个离线图标来自 MIT 许可的 SkyShards `public/shardIcons`，审核 commit 为 `9688031dbc4e726168ffceb0f44884ff26e6e728`；生成时严格按 QCA 的 320-Shard 目录筛选，并排除额外的 Rainbug 资源。

### 安全边界

- Shard 目录、回退图标、物品模型与界面均随模组打包且只读。QCA 运行时不会请求 Wiki、API 或图标，也不会发送聊天、服务器命令、数据包、菜单点击、Fusion 或自动操作。

## [2.5.4] - 2026-08-09

适用于 Minecraft 26.1.2 的 Alpha 更新。

这是 `1.5.3` 最新发布基线之后的 `2.5.x` Alpha 开发线起点。发布通道仍为 Alpha；本次只重编号 1.5.3 之后的版本线。

### 新增

- 在“物品与菜单”加入受 JEI 信息结构启发、完全离线的 Attribute Shard Fusion Guide。
- 支持按原始 Shard 名称、ID、属性、品质、分类、家族和 Skill 搜索；提供 Recipes/Uses 标签、有序输入组合、浏览历史、分页、客户端已观察原生物品图标、输入/输出数量、特殊产量及 Pure Reptile 双倍产出概率。
- 新增本地 `/qshard [英文查询]` 界面命令、设置中的“打开指南”及默认未绑定的键盘/鼠标组合键；三种入口均不发送服务器载荷。

### 调整

- Alpha 版本提升至 `2.5.4`，发布文件名为 `QCloudy_Addition-alpha-2.5.4-26.1.2.jar`。
- Shard 目录改为严格匹配 Hypixel 官方 Bazaar 的 320 项允许列表：包含 Anteater、Zombuddy、Troodon 与 Ghost Crab；Goldolot 为 `R92`；Rainbug 因不在官方 Bazaar Shard 产品集合中而排除。
- 完整保留 Attribute Fusion 输入顺序、最多三个可选输出、Chameleon 数字 ID/品质进位，以及 Wiki 记录的消耗与产量规则。
- 当 ID Fusion 与 Special Fusion 产出同名 Shard 时保留两个独立输出槽，因为玩家可选择的产量仍分别为 `x1` 与 `x2`。

### 删除

- 从实现、配置、测试和当前文档中彻底删除槽位锁定、Storage 覆盖与菜单中键转换。

### 安全边界

- 指南只读取随模组提交的离线 JSON；可选的材质包适配图标只来自客户端已经观察到的 ItemStack。运行时不会请求 Wiki/API/网络，不会点击菜单、执行 Fusion、发送命令/聊天或自动操作。

## [1.5.1] - 2026-08-06

Minecraft 26.1.2首个公开发布候选版本。

### 新增

- Dwarven Mines单层地图与Glacite Tunnels三层地图。
- Mining Commission、三种Powder、HOTM槽位及Crimson Isle任务追踪。
- Torrhus Chapter/资源、Tree Critter、Miria Contest、Benefactor及本人Tree Gift追踪。
- Critter Safari Dashboard/Critterdex、Cold/篝火、Doomspiral、Warden、Sparkling、Floor Drop、Quest Item、Wumpa、Snoozle、Safari Belt与Critter高亮。
- Beeheemoth轮廓、生成光柱与空间声音控制。
- Lasso REEL音效和中央预警系统。
- 支持真实头颅、皮肤、经验、溢出等级及宠物用品的Pet HUD。
- Ender Dragon高亮、Chat Peek、时间戳、光标记忆和自定义传送音效。
- 中英双语设置和每HUD独立编辑器。
- 手动重连按钮、`/th`与`/helia`客户端快捷命令。

### 修复

- 删除会显示错误/模糊宠物图标的旧PNG回退。
- 满级宠物只隐藏多余至满级经验，不再丢失宠物用品。
- 粗体、长任务与宠物文字不再溢出或显示省略号。
- Safari捕捉Armor Stand不再获得Critter轮廓。
- 修复Wumpa队友Loot Share、生成后HUD及Ravager身体路线。
- 修复四项Safari Belt Milestone及按账号/Profile保存。
- 修复Helia Chapter、Benefactor、Whispers、Essence、Forest Fortune和Sweep获取/保存。
- 使用本人归属状态机修复附近玩家及重复Tree Gift提醒。
- 清除最后一个已弃用的已加载区块调用，扫描范围不变。

### 调整

- 项目与原版按键分类统一为QCloudy_Addition / QCloudy Addition。
- 设置分为通用、地图、挖矿、砍树、狩猎、Safari、Crimson Isle、Combat、Pets、Chat与Inventory，每项功能只有一个分类。
- AOTE/AOTV默认保留原声，并提供声音/音量/音调选择。
- 所有预警音量默认统一为64%。

### 删除

- “全部”设置分类。
- Golden Dragon/Dragon's Lair寻找功能。
- 重复开关、右键提示和独立热键捕获菜单。
- Firmament运行依赖与旧宠物PNG选择。
